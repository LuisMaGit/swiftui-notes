import NotesCoreUI
import SwiftUI

struct NoteLoading: View {
    var body: some View {
        NLoader()
    }
}

struct NoteLoading_Previews: PreviewProvider {
    static var previews: some View {
        NoteLoading()
    }
}
